import React from 'react';
import ReactDOM from 'react-dom';
import RouterView from './app/navigation/RouterView';
// import reportWebVitals from './reportWebVitals';

ReactDOM.render(
  <React.StrictMode>
    <RouterView />
  </React.StrictMode>,
  document.getElementById('root'),
);

// reportWebVitals(console.log);
